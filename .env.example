# Optional: OpenRouter API key. Only routes with observed zero pricing are admitted.
OPENROUTER_API_KEY=your_openrouter_api_key_here

# Optional: local SQLite control-plane path (defaults to ./free-fleet.db for
# account/free-fleet commands; Career Fleet uses --db career_fleet.db).
BULK_LANES_DB=free-fleet.db
